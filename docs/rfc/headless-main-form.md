---
title: "Главная zero-config форма для headless-приёма заявок"
status: draft
created: 2026-09-12
---

# RFC: headless main form — главная zero-config форма

## Вводные

Сегодня, чтобы принять заявку с внешнего фронтенда, нужно сначала завести форму: создать CPT-форму в админке (и узнать её post ID) либо зарегистрировать code-форму через `fforms_add_api_route()`. Для простого «отправь мне заявку с Astro-сайта» это лишний шаг. Предлагается добавить **встроенную главную форму** с фиксированной схемой (`name`, `email`, `phone`, `message`, `attachments`), отдельный лояльный маршрут `POST /wp-json/fforms/v1/main` с плоским camelCase-payload и **тип заявки как таксономию** `fform_type`, по которому заявки группируются и фильтруются. Работает сразу после активации плагина.

## Назначение и цели

- Дать headless-фронтенду (AstroJS и подобные) рабочий endpoint приёма заявок без предварительного создания формы.
- Различать заявки по бизнес-смыслу («Заявка на консультацию», «Заявка на вступление в клуб»), не заводя под каждый сценарий отдельную форму: тип приходит строкой с фронтенда и становится термином таксономии.
- Не ломать существующий контракт: `POST /submit` с `form_id`/`form_key` и объектом `fields` остаётся ровно таким, каким его используют блок, шорткод, публичная страница и code-формы.
- Принимать «как есть» произвольный контекст запроса (`ref`, `meta`, `customFields`, `userId`), не ослабляя серверную валидацию схемы.
- Принимать файлы во вложениях, не открывая при этом анонимную загрузку в media library.

Заинтересованные стороны: разработчики headless-фронтендов, интеграторы FForms, ревьюеры REST-контракта и безопасности.

## Составляющие и особенности

### 1. Встроенная главная форма

`FForms\Registry\Main_Form` собирает `Form_Ref` в памяти: `post_id = 0`, `key = 'main'`, `source = 'builtin'`, схема из четырёх полей (`name` text, `email` email, `phone` tel, `message` textarea) — все необязательные. Ключ `main` резервируется: `fforms_add_api_route( 'main', … )` возвращает `WP_Error` (`fforms_reserved_key`). Главная форма не создаёт записей в БД, не редактируется в админке и не встраивается блоком.

`Form_Locator::resolve( 'main' )` возвращает этот `Form_Ref`, поэтому существующие маршруты `GET /forms/main` и `GET /forms/main/schema` начинают работать без отдельного кода, а `GET /forms` отдаёт главную форму в общем списке с `source: 'builtin'` и `mode: 'headless'` — фронтенд может сгенерировать разметку по схеме.

Серверное правило против пустых заявок: непустым должно быть хотя бы одно из `email`, `phone`, `message`, иначе 422 `fforms_empty_submission`.

### 2. Тип заявки: таксономия `fform_type`

Значение, которое фронтенд передаёт как `formType` (алиасы `formId`, `form_type`, `form_id` — одно и то же поле), несёт бизнес-смысл заявки, а не только адрес формы. Оно хранится термином непубличной таксономии `fform_type`, зарегистрированной для `fform_entry` и `fform`.

- Регистрация: `public => false`, `publicly_queryable => false`, `show_ui => true`, `show_in_rest => false`, `hierarchical => false`, `show_admin_column => true`. Метка «Тип заявки», экран внутри меню FForms. Существующая мета формы `_fforms_type` (`contact`/`lead`) остаётся как есть и с таксономией не смешивается.
- Нормализация: `sanitize_key`, паттерн `^[a-z0-9_-]{1,32}$`; иначе 422 `fforms_invalid_form_type`.
- Upsert по slug: если термина нет — создаётся с `name = slug`. Администратор переименовывает термин в человекочитаемое «Заявка на консультацию», slug остаётся ключом связи, поэтому фронтенд ничего не меняет.
- Ограничение роста: не больше 50 автосозданных типов (фильтр `fforms_max_form_types`). При превышении лимита термин не создаётся, входное значение сохраняется в мету `_fforms_form_type_raw`, заявка принимается и не теряется.
- Строгий режим: настройка «Принимать только существующие типы». При включении неизвестный slug даёт 422 `fforms_unknown_form_type` — для сайтов с фиксированным списком типов и как защита от засорения таксономии.

**Форма тоже получает свой тип.** При публикации CPT-формы плагин создаёт термин `fform_type` и связывает их в обе стороны:

- slug термина — `post_name` формы (при занятом slug добавляется числовой суффикс, как в ядре), `name` — заголовок формы. Slug стабилен: переименование формы обновляет только `name`, поэтому фронтенд продолжает слать прежнее значение.
- Термин назначается самой форме (одна форма — один тип), а в мете термина хранится `_fforms_form_id` — ID формы. Это и есть двусторонняя связь: от формы к её заявкам и от типа к форме.
- Удаление формы не удаляет термин — заявки сохраняют классификацию; `_fforms_form_id` очищается, а на экране термина остаётся пометка, что формы больше нет.
- Кнопка «Смотреть заявки» у формы ведёт на `edit.php?post_type=fform_entry&fform_type=<slug>`, на экране термина — обратная ссылка на форму. Это закрывает пункт ROADMAP про переход от формы к её ответам.
- Побочный, но важный эффект: slug термина становится **стабильным строковым ключом CPT-формы**. `Form_Locator::resolve()` получает третий путь — по slug термина с непустым `_fforms_form_id`, поэтому headless-фронтенду больше не нужен post ID, который отличается между dev/stage/prod. Порядок резолва: число → post ID, зарегистрированный ключ code-формы, slug термина с формой, иначе — встроенная главная форма.

**Адресация и классификация независимы.** Одно значение обрабатывается в два шага: `Form_Locator::resolve()` ищет форму в порядке выше (post ID, ключ code-формы, slug термина); если форма найдена — заявка идёт в неё, если нет — во встроенную главную форму. Затем то же значение нормализуется в slug и назначается entry термином `fform_type`. Так `formType: "consultation"`, совпавший с ключом code-формы, и адресует, и классифицирует; `formType: "club_membership"`, которому не соответствует ни одна форма, остаётся только типом.

Заявки из блока, шорткода и `/submit` тоже участвуют: entry наследует термины `fform_type` своей CPT-формы, а code-форма может задать тип необязательным аргументом `type` в `fforms_add_api_route()`. Если у формы тип не задан, термин не назначается — поведение существующих форм не меняется.

### 3. Маршрут `POST /fforms/v1/main`

Принимает `application/json` и `multipart/form-data`. Параметры (все опциональны, кроме правила из §1):

| Параметр | Тип | Назначение |
| --- | --- | --- |
| `formType` (`formId`, `form_type`, `form_id`) | string\|int | Тип заявки и одновременно адрес формы — см. §2. По умолчанию заявка идёт во встроенную главную форму без термина. |
| `name`, `email`, `phone`, `message` | string | Поля схемы; проходят обычную `Schema::validate_submission()`. |
| `customFields` | object | Поля вне схемы — см. §4. |
| `meta` | object | Произвольный контекст запроса — см. §4. |
| `ref` | string | `utm_source` или `ref` из query string. |
| `userId` | int | Идентификатор пользователя на стороне клиента. |
| `attachments` | file[] | Только в `multipart`, см. §5. |
| `_hp` | string | Honeypot нового маршрута. Заполнен — 200 без entry, как в `/submit`. |
| `source` | string | Как в `/submit`; при отсутствии берётся `Referer`. |

Если передано несколько алиасов с разными значениями — 400 `fforms_form_ref_conflict`.

Неизвестные ключи верхнего уровня не отбрасываются, а трактуются как `customFields` с теми же лимитами (§4): `{ "email": …, "website": "https://acme.dev" }` сохранит `website` в `_fforms_custom`. Поэтому honeypot на этом маршруте называется `_hp`, а не `website`: в плоском payload имя `website` — нормальное пользовательское поле (сайт компании), и резервировать его под ловушку значило бы молча терять данные. На `/submit` honeypot остаётся `website` — контракт блока и шорткода не меняется.

Обработка — тот же пайплайн, что в `base.md` §6: лимит размера тела → резолв формы и типа → honeypot (200 без entry) → rate limit → валидация схемы → entry → термин `fform_type` → письма → `fforms_entry_created`. Общий код выносится из `REST_Controller::submit()` в приватный `process( Form_Ref $form, array $fields, WP_REST_Request $request )`; новый маршрут отличается только маппингом плоского payload в `fields` и обработкой типа, extras и вложений. Ответы те же: 201 с `entry_id` (плюс `form_type`), 422 с ошибками по полям (ключи — camelCase, как пришли), 400/404/413/429/500.

Rate limit считается по паре «ссылка на форму + IP» (`code:main` для главной формы) — существующий механизм, ключ главной формы не пересекается с CPT-формами.

### 4. Данные вне схемы

Валидация схемы не ослабляется: `_fforms_data` по-прежнему содержит только прошедшие allowlist поля. Всё остальное сохраняется отдельными метами entry:

- `_fforms_custom` — `customFields`: ключи через `sanitize_key`, значения только скаляры (нескалярные отбрасываются), максимум 20 ключей, 2 000 символов на значение.
- `_fforms_meta` — `meta`: JSON, глубина ≤ 3, ≤ 8 KiB после кодирования; при превышении — 422 `fforms_meta_too_large`.
- `_fforms_ref` — `sanitize_text_field`, ≤ 200 символов.
- `_fforms_user_id` — `absint`; значение приходит от клиента и никаких прав не даёт. В админке показывается ссылкой на профиль только если такой пользователь существует.

### 5. Вложения

Файлы принимаются только в `multipart/form-data` как `attachments[]`; `attachments` в JSON-теле — 400 `fforms_attachments_require_multipart`.

- Лимиты: ≤ 3 файлов, ≤ 5 MiB каждый; общий лимит тела для этого маршрута — отдельный фильтр `fforms_main_form_max_upload_bytes` (по умолчанию 16 MiB), поскольку `fforms_max_request_bytes` (256 KiB) остаётся лимитом JSON-запросов.
- Тип проверяется `wp_check_filetype_and_ext()` и `finfo` по содержимому, а не по расширению. Allowlist: pdf, png, jpg/jpeg, webp, txt, csv, docx, xlsx. Исполняемые и активные типы (php, phtml, html, js, svg) запрещены всегда, фильтром типов их расширить нельзя.
- Хранение вне media library: `uploads/fforms-private/<Y>/<m>/` с `index.html`, `.htaccess` (deny from all) и случайным именем файла. Публичного URL у файла нет; attachment-записи не создаются.
- Выдача: `GET /fforms/v1/entries/{id}/attachments/{index}` с `manage_options`, отдаёт файл потоком с `Content-Disposition: attachment`. Ссылки на этот маршрут идут в письмо и на экран entry; сами файлы к письму не прикрепляются.
- Файлы записываются на диск **после** успешной валидации и создания entry — невалидные и rate-limited запросы не оставляют мусора. Удаление entry (`before_delete_post`) удаляет его файлы.
- Тумблер в настройках «Разрешить вложения» — **по умолчанию выключен**; при выключенном тумблере `multipart`-запрос с файлами получает 400 `fforms_attachments_disabled`. Причина: анонимная загрузка файлов расширяет поверхность атаки и расход диска, а `base.md` §10 требует осознанного решения по хранению. Остальная часть главной формы работает zero-config и без этого тумблера.

### 6. Админка, фильтрация и экспорт

- Колонка «Тип заявки» в списке `fform_entry` (`show_admin_column`) и блок «Дополнительно» на экране entry: `ref`, `user_id`, `customFields`, `meta`, список вложений.
- Выпадающий фильтр по типу рядом с существующим фильтром по форме (`restrict_manage_posts` + `parse_query`), работает вместе с фильтрами формы и статуса.
- `GET /fforms/v1/entries` получает параметр `form_type` (slug), комбинируемый с `form_id`, `form_key` и `status`.
- CSV: колонки `form_type`, `ref`, `user_id`, `custom_fields` (JSON), `meta` (JSON), `attachments` (имена файлов); экспорт можно ограничить типом. В письмо уведомления тип и extras попадают отдельным блоком после полей формы.

### 7. Страница обзора `fforms-dashboard`

Обзорная страница переезжает с `admin.php?page=fforms` на `admin.php?page=fforms-dashboard` и становится точкой входа для headless-интеграции.

- Верхнеуровневый slug меню остаётся `fforms`: он используется как `show_in_menu` обоими CPT и как parent страниц `fforms-settings` и `fforms-export`. Переименовывается только страница обзора — `add_submenu_page( 'fforms', …, 'fforms-dashboard', … , 0 )`, дубликат снимается `remove_submenu_page( 'fforms', 'fforms' )`.
- `load-toplevel_page_fforms` редиректит `admin.php?page=fforms` на новый адрес, поэтому клик по верхнему пункту меню, старые закладки и ссылки из документации продолжают работать.
- Новый блок «Приём заявок через API»: реальный URL маршрута (`rest_url( 'fforms/v1/main' )`), состояние тумблеров (вложения, строгий режим типов, разрешённые origins) и список существующих типов со slug — чтобы разработчик фронтенда копировал рабочие значения, а не сверялся с документацией.
- Там же 4 примера запросов с кнопкой «Скопировать»:
  1. **Контактная форма** — `name`, `email`, `message`, без `formType`; заявка попадает в главную форму.
  2. **Заявка на консультацию** — `formType: "consultation_request"`, `name`, `phone`, `email`; показывает, как заводится тип.
  3. **Email + сайт компании** — `email` и `website`; показывает, что нестандартное поле уезжает в `customFields` без настройки схемы.
  4. **Заявка с файлом** — `curl` с `multipart/form-data` и `attachments[]`; отображается с пометкой, если тумблер вложений выключен.

### 8. CORS и настройки

Главная форма резолвится с `origins = []`, поэтому по умолчанию CORS-заголовки не отправляются — кросс-доменный запрос требует явного allowlist. Добавляется поле настроек «Разрешённые origins главной формы» (список через запятую) плюс существующий фильтр `fforms_allowed_origins( array $origins, ?Form_Ref $form )`. Preflight `OPTIONS` для нового маршрута работает так же, как для `/submit` (см. `api-route-headless-cms-mode.md` §6). Zero-config относится к созданию формы, не к CORS: для внешнего домена origin указать придётся.

Уведомления главной формы следуют общему правилу плагина — по умолчанию выключены; получатели задаются в настройках (по умолчанию `admin_email` при включении).

### 9. Вне объёма

Редактирование главной формы в админке, её рендер блоком или на публичной странице, иерархические типы заявок и настройки уведомлений на уровне типа, per-form токены и подписи запросов, webhooks, `v2` REST API, приём вложений на существующем `/submit`.

## Критерии приемки (checklist)

- [ ] `POST /fforms/v1/main` без `formType` и с одним `message` создаёт entry: 201, `entry_id`, `message`; в админке entry подписан «Главная форма (API)».
- [ ] Запрос без `email`, `phone` и `message` возвращает 422 `fforms_empty_submission` и не создаёт entry.
- [ ] `formType: "consultation_request"`, которого ещё нет, создаёт термин `fform_type` и назначает его entry; вторая заявка с тем же значением переиспользует термин, а не создаёт дубль.
- [ ] Переименование термина в админке в «Заявка на консультацию» не ломает привязку последующих заявок — связь идёт по slug.
- [ ] `formType` с недопустимыми символами или длиннее 32 символов — 422 `fforms_invalid_form_type`; при включённом строгом режиме неизвестный slug — 422 `fforms_unknown_form_type` без создания entry.
- [ ] После достижения лимита автосозданных типов заявка сохраняется без термина, входное значение доступно в `_fforms_form_type_raw` и видно на экране entry.
- [ ] `formType` с ключом зарегистрированной code-формы, ID опубликованной CPT-формы или slug её термина направляет заявку в эту форму и проставляет термин; `formId` и `formType` с разными значениями — 400 `fforms_form_ref_conflict`.
- [ ] Публикация новой CPT-формы создаёт термин `fform_type` со slug = `post_name` и `name` = заголовок, назначает его форме и пишет `_fforms_form_id` в мету термина; повторное сохранение формы дублей не плодит.
- [ ] Переименование формы меняет `name` термина и не меняет его slug; форма с занятым slug получает термин с числовым суффиксом.
- [ ] Кнопка «Смотреть заявки» у формы открывает список `fform_entry`, отфильтрованный по её термину; с экрана термина есть обратная ссылка на форму.
- [ ] Удаление формы не удаляет термин и не теряет привязку заявок; `_fforms_form_id` очищается.
- [ ] Заявка из блока, шорткода и `POST /submit` получает термин своей формы; форма без термина сохраняет entry без типа.
- [ ] Список `fform_entry` показывает колонку «Тип заявки» и фильтруется по типу вместе с фильтром по форме и статусу; `GET /entries?form_type=…` и CSV-колонка `form_type` возвращают те же заявки.
- [ ] `GET /fforms/v1/forms` содержит главную форму (`key: "main"`, `source: "builtin"`); `GET /forms/main/schema` отдаёт четыре поля; `fforms_add_api_route( 'main', … )` возвращает `WP_Error`.
- [ ] Rate limit даёт 429; превышение лимита тела — 413; `POST /submit` с honeypot `website` ведёт себя как прежде.
- [ ] `customFields` с 25 ключами и вложенным объектом сохраняет 20 скалярных ключей в `_fforms_custom`; `_fforms_data` содержит только поля схемы; `meta` глубиной 5 или больше 8 KiB — 422 `fforms_meta_too_large`.
- [ ] `ref` и `userId` видны на экране entry и в CSV; несуществующий `userId` не рендерится ссылкой.
- [ ] При выключенном тумблере вложений `multipart`-запрос с файлом возвращает 400 `fforms_attachments_disabled`.
- [ ] При включённом тумблере: pdf и png сохраняются, `.php` и `.svg` отклоняются (в том числе переименованные в `.pdf`), 4-й файл и файл больше 5 MiB отклоняются.
- [ ] Файл недоступен по прямому URL в `uploads/`; `GET /entries/{id}/attachments/{index}` отдаёт его администратору и отказывает анонимному запросу; удаление entry удаляет файлы с диска.
- [ ] Неизвестный ключ верхнего уровня (`website`) сохраняется в `_fforms_custom`, а не теряется; заполненный `_hp` даёт 200 без entry, писем и термина.
- [ ] Запрос с origin из настроек получает `Access-Control-Allow-Origin`, с чужого — нет; `OPTIONS` возвращает 204.
- [ ] Обзор открывается по `admin.php?page=fforms-dashboard`; `admin.php?page=fforms` редиректит на него; пункты «Формы», «Ответы», «Настройки», «Экспорт CSV» остаются в том же меню и в том же порядке.
- [ ] На странице обзора показаны URL маршрута, состояние тумблеров, список типов и 4 примера запросов; пример с вложением помечен как недоступный при выключенном тумблере.
- [ ] `POST /submit` из блока, шорткода и публичной страницы не изменился; `php -l`, `npm run lint:js`, `npm run lint:css`, `npm run build` проходят.
- [ ] `docs/specs/api-route-headless-cms-mode.md` и `docs/specs/base.md` дополнены описанием главной формы, таксономии `fform_type`, новых мет entry и вложений; в `LOG.md` есть запись.

## Дорожная карта

1. `Main_Form`, резерв ключа `main`, резолв в `Form_Locator`, главная форма в `GET /forms(/main)(/schema)`.
2. Таксономия `fform_type`: регистрация для `fform_entry` и `fform`, нормализация и upsert по slug, лимит автосоздания, строгий режим, аргумент `type` у code-форм.
3. Двусторонняя связь формы и термина: создание термина при публикации формы, `_fforms_form_id` в мете термина, синхронизация названия, резолв формы по slug термина, кнопки перехода форма ↔ заявки.
4. Выделение общего пайплайна `process()` из `REST_Controller::submit()` и новый маршрут `POST /main` с маппингом плоского payload и разрешением алиасов `formType`/`formId`.
5. Extras: меты `_fforms_custom`, `_fforms_meta`, `_fforms_ref`, `_fforms_user_id`.
6. Админка и экспорт: колонка и фильтр по типу, блок «Дополнительно», параметр `form_type` в `GET /entries`, новые колонки CSV.
7. Настройки: origins главной формы, получатели уведомлений, строгий режим типов, тумблер вложений; CORS-ветка для нового маршрута.
8. Вложения: multipart-ветка, проверка типов, приватное хранилище, защищённая выдача, удаление вместе с entry.
9. Страница обзора: переезд на `fforms-dashboard` с редиректом со старого адреса, блок «Приём заявок через API» и примеры запросов.
10. Документация (`base.md`, `api-route-headless-cms-mode.md`, пример AstroJS) и ручная проверка чек-листа.

Шаги 1–4 блокируют всё остальное; 3 и 5 независимы между собой; 8 требует 4 и 7; 9 идёт после 7, чтобы примеры показывали фактические настройки.

## Предпосылки

Главная zero config форма для headless-режима, которая автоматически подхватывает опубликованную форму и предоставляет её через REST API без необходимости ручной настройки.

- имеет какой то базовый wp json rest api route
- принимает ряд базовых параметров, таких как 
    - `formId` - key string field
    - `userId` - id user - optional
    - `meta` - optional context for the request - any json field
    - `ref` - reference to the form - optional - utm_source query string or just ref= query string
    - email - optional
    - name - optional
    - message - optional
    - phone - optional
    - attachments - optional - array of file objects
    - customFields - optional - any additional custom fields as a JSON object - key-value pairs

## Итого и рекомендации

Главная форма — это встроенный `Form_Ref` с фиксированной схемой, а не запись в БД и не «подхват» произвольной опубликованной формы: так поведение endpoint'а одинаково на dev/stage/prod и не зависит от того, какие формы завёл редактор. Различать сценарии («консультация», «вступление в клуб») предлагается не отдельными формами, а типом заявки в таксономии `fform_type`: фронтенд передаёт стабильный slug, админ даёт ему человекочитаемое имя, а список заявок получает привычную для WordPress колонку и фильтр. Каждая CPT-форма заводит собственный термин и связывается с ним в обе стороны, поэтому slug заодно закрывает давнюю проблему headless-режима: у обычной формы появляется стабильный строковый ключ вместо post ID, который отличается между окружениями. Лояльный payload живёт на отдельном маршруте `POST /main`, поэтому строгий контракт `/submit` остаётся нетронутым. Данные вне схемы сохраняются, но отдельно от валидированных — граница «что прошло серверную валидацию» не размывается.

## Открытые вопросы

- Автосоздание терминов включено по умолчанию (zero-config), строгий режим — опция. Подтвердить, что по умолчанию нужен именно лояльный вариант, а не наоборот.
- Нужен ли публичный `GET /fforms/v1/form-types` для фронтенда (список типов с названиями), или типы остаются внутренним справочником админки.
- Вложения заметно увеличивают объём RFC и поверхность атаки. Если на реализации станет тесно — вынести §5 в отдельный RFC `main-form-attachments`, оставив здесь контракт «attachments отклоняются».
- Тумблер вложений по умолчанию выключен ради безопасности, что формально нарушает «zero config». Подтвердить или включить по умолчанию с более жёсткими лимитами.
- Нужен ли главной форме `store_entries: false` (только письмо, без сохранения) — тот же вопрос остался открытым в `use-fforms-as-headlesscms-backend.md`.
